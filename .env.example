# Supabase configuration for backend/auth storage
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SECRET_KEY=your-secret-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (alternative server key)
# SUPABASE_PUBLISHABLE_KEY=your-publishable-key (not recommended for server use)
# For client-side Next.js code only (never use server-only secrets here)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

JWT_SECRET=replace-with-a-long-random-secret

# Legacy MongoDB settings removed after Supabase migration
#MONGO_URI=mongodb://127.0.0.1:27017/whatsapp_messaging_bot
